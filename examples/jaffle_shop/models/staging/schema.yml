version: 2

models:
  - name: stg_customers
    description: Cleaned, renamed view over raw_customers.
    columns:
      - name: customer_id
        description: Primary key.
        data_tests:
          - unique
          - not_null
      - name: first_name
        data_tests:
          - not_null
      - name: last_name
        data_tests:
          - not_null
